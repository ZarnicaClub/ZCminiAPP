# Инвентаризация production-данных (24.08.2026)

Источники: `data/default_db.sql` (DDL, PostgreSQL 17.10), `data/orders.csv`,
`data/calls.csv`. Все три — выгрузки из production БД `default_db`.

## Таблицы и объём

| Таблица | Строк | Действие при миграции |
|---|---|---|
| `orders` | 30 | переносим в новую схему, линкуем к `clients` |
| `order_items` | 30 | **удаляем** (все строки одинаковые: «Игра в клубе ЗарницаКлаб», 5000₽, qty 1) |
| `calls` | 223 | переносим с дедупликацией |

## Дедупликация звонков (решение заказчика)
- 223 звонка → **213 уникальных** `sha256` → 10 дублей (один и тот же MP3
  импортирован дважды под разными `email_id`).
- Правило: оставляем 1 звонок на уникальный `sha256`.

## Статусы звонков
- `matched`: 36
- `unmatched`: 187
- `ambiguous`: 0

## Клиенты (`clients`)
- Уникальных телефонов в `orders`: 30
- Уникальных телефонов в `calls`: 126
- Пересечение: 20
- **`clients` = объединение = 136** уникальных телефонов.
- Имя/email — из заказа, где есть; у клиентов только из звонков — только телефон.

## Замечания
- `customer_phone` в `orders` уже нормализован (`7XXXXXXXXXX`).
- 187 unmatched-звонков невидимы в текущем Mini App → в новом CRM (этап 5)
  обязателен экран «Клиенты / Все звонки» с ручным связыванием (MANUAL_MATCH).
- MP3 в S3 не перезаливаем; контентно-адресуемые ключи — только для новых звонков.
- `email_id` в `calls` — IMAP-позиционные номера, а не UID (дефект, фиксим в новом worker).
